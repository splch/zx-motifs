[project]
name = "zx-discovery"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "qiskit>=2.3.0",
    "pyzx>=0.9.0",
    "networkx>=3.6",
    "numpy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
